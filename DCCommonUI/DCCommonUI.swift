//
//  dclife
//

import UIKit
import DCCommon

extension Common {
    public enum UI {
        public enum Flow {}
        public enum Model {}
        public enum Service {}
        public enum View {}
        public enum Controller {}
        public typealias Factory = CommonUIFactory
    }
}
